pkgname="ruby"
pkgversion="2.1.5"
pkgfile="${pkgname}-${pkgversion}.tar.gz"
pkguri="http://cache.ruby-lang.org/pub/ruby/2.1/${pkgfile}"
pkgmd5="df4c1b23f624a50513c7a78cb51a13dc"

install_package() {
  cd ${pkgbuild_path}
  ./configure \
    --prefix="${BREW_PREFIX}/rubies/${pkgname}-${pkgversion}" \
    --disable-install-doc \
    --disable-install-rdoc
  make
  make install
}
