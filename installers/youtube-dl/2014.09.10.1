pkgname="youtube-dl"
pkgversion="2014.09.10.1"
pkgfile="${pkgname}"
pkguri="https://yt-dl.org/downloads/${pkgversion}/${pkgfile}"
pkgmd5="bd84dc7459eebba5c070a538db1c1192"

extract_source() {
  echo ''
}

install_package() {
  mkdir -p ${BREW_PREFIX}/bin
  cp $(pkgdownload_path) ${BREW_PREFIX}/bin
  chmod +x ${BREW_PREFIX}/bin/${pkgname}
}
